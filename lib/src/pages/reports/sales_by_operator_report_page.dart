import 'package:flutter/material.dart';

class SalesByOperatorReportPage extends StatelessWidget {
  const SalesByOperatorReportPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
