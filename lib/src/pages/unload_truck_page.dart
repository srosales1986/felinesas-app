import 'package:flutter/material.dart';

class UnloadTruckPage extends StatelessWidget {
  const UnloadTruckPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Registro de descargas'),
      ),
      body: Center(
        child: Text('Acá va el registro de descargas'),
      ),
    );
  }
}
