import 'package:flutter/material.dart';

class SalesByUserByDate extends StatelessWidget {
  const SalesByUserByDate({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
